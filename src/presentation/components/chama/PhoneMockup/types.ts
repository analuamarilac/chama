import type { ReactNode } from "react";

interface PhoneMockupProps {
	children: ReactNode;
	/** Rótulo lido por tecnologia assistiva no lugar da moldura decorativa. */
	label: string;
}

export type { PhoneMockupProps };
